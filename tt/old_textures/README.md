# Old Textures

These are the original textures from the game. The textures now found in `tt/textures/` are upscaled versions of these originals.

This folder is not included in the build and exists only as a reference in the repository and as a simple way to go back to the old textures.
